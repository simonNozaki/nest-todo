<!DOCTYPE html>
<html>
    <head>
        <title> nest-todo </title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    </head>
    <body>
        <div class="container">
            <h1> TODOリスト </h1>
            <div class="row">
                <div class="col s6">
                    {{#each tasks}}
                    <ul class="collection">
                        {{#each this}}
                            <li class="collection-item"> {{this}} </li>
                        {{/each}}
                    </ul>
                    {{/each}} 
                </div>
                <div class="col s6">
                    <form action="tasks" method="POST">
                        <input class="input-field" type="text" name="title" placeholder="タイトル...">
                        <input class="input-field" type="text" name="description" placeholder="説明...">
                        <input class="input-field" type="date" name="deadline" placeholder="期限...">
                        <input class="btn waves-effect waves-light" type="submit" value="追加する">                
                    </form>
                </div>
            </div>
        </div>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    </body>
</html>